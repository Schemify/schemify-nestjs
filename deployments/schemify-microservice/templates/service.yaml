apiVersion: v1
kind: Service
metadata:
  name: {{ include "schemify-microservice.fullname" . }}
  labels:
    {{- include "schemify-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "schemify-microservice.selectorLabels" . | nindent 4 }}
