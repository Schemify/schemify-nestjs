apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "schemify-microservice-chart.fullname" . }}-test-connection"
  labels:
    {{- include "schemify-microservice-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "schemify-microservice-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
